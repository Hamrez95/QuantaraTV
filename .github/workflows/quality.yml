name: Quality
on:
  pull_request:
  push:
    branches: [main, dev]
permissions:
  contents: read
jobs:
  static:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Static validation
        run: bash tests/static_checks.sh
